<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      DJProxy platform theme. The app itself is 100% Jetpack Compose (Material 3 via
      ui/theme/Theme.kt); this XML theme only controls the window before Compose draws
      (splash-to-content background) and system bar chrome, so it must not depend on any
      AppCompat/Material Components library that isn't a project dependency.
    -->
    <style name="Theme.DJProxy" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/djp_bg</item>
        <item name="android:statusBarColor">@color/djp_bg</item>
        <item name="android:navigationBarColor">@color/djp_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:colorPrimary">@color/djp_accent_cyan</item>
        <item name="android:colorAccent">@color/djp_accent_indigo</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="q">false</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="q">false</item>
    </style>

    <!--
      Launch-time splash (androidx.core.splashscreen). This is the *manifest-declared* activity
      theme so the OS/compat lib can paint the branded frame before Compose's first frame — it
      then hands off to Theme.DJProxy via postSplashScreenTheme the moment installSplashScreen()
      runs in MainActivity.onCreate. Brand art: the tri-tone shield + power-ring emblem in
      res/drawable/splash_icon.xml — a density-independent VectorDrawable (reuses the launcher's
      brand gradient), replacing the former drawable-*/splash_icon.png rasters so the mark stays
      retina-crisp at every density and survives the API 31+ circular splash mask (2/3 safe zone).
    -->
    <style name="Theme.DJProxy.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/djp_splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/djp_splash_bg</item>
        <item name="postSplashScreenTheme">@style/Theme.DJProxy</item>
    </style>
</resources>
